# Complete UsenetSync Requirements
# ALL required modules for full functionality

# ============ CORE BACKEND ============
# Web Framework & API
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# ============ DATABASE ============
# PostgreSQL (REQUIRED for production)
psycopg2-binary==2.9.9
# Redis (for caching)
redis==5.0.1
# Database migrations
alembic==1.12.1

# ============ USENET (CRITICAL!) ============
# NNTP client for Usenet operations
pynntp==1.0.2

# ============ SECURITY ============
# Encryption and hashing
cryptography==41.0.7
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
# Ed25519 keys
PyNaCl==1.5.0

# ============ NETWORKING ============
httpx==0.25.2
websockets==12.0
aiofiles==23.2.1
# yEnc encoding for Usenet
yenc==0.4.0

# ============ MONITORING ============
psutil==5.9.6
prometheus-client==0.19.0

# ============ UTILITIES ============
python-dotenv==1.0.0
click==8.1.7
rich==13.7.0
pyyaml==6.0.1

# ============ DEVELOPMENT ============
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
mypy==1.7.1
ruff==0.1.6

# ============ ADDITIONAL ============
# For file operations
watchdog==3.0.0
# For async operations
asyncio==3.4.3
# For data processing
numpy==1.24.3
# For compression
zstandard==0.22.0